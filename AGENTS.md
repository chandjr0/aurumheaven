# Aurum Haven — agent notes

- Prefer non-destructive git history. Avoid force-pushing, rebasing, amending, or squashing commits that are already on the remote shared branch unless the user explicitly requests it.
- Keep the connected branch in a working, buildable state.
- Do not change business copy, images, or branding unless the user asks.
